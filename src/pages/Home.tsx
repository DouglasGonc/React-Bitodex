import styled from "styled-components";

const Banner = styled.main`
    height: 860px;
`;

const Home: React.FC = () => {
    return (
        <>
            <Banner>HOME</Banner>
        </>
    );
};

export default Home;